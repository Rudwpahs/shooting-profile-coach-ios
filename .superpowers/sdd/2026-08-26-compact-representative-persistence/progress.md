# SDD ledger — plan: docs/superpowers/plans/2026-08-26-compact-representative-persistence.md

- Workspace isolation: remote branch `codex/representative-dual-view-4d`; local snapshot has no Git metadata, so remote updates use fast-forward GitHub tree/commit/ref operations.
- Approved design: `docs/superpowers/specs/2026-08-26-compact-representative-persistence-design.md`.
- Remote state before this delivery: `main` at `c09eb573cc9bed122c04f907e5a83d2c216973ba`; feature branch at `762bdfdd5d62377791cf91c4f43be962197b3e0a`, 55 commits ahead and 0 behind.
- Dependency evidence: repository `node_modules` remains quarantined because it is not a clean install of the committed lockfile. Local commands using it are diagnostic; the clean GitHub Actions run is the completion authority for install, lint, and Expo export.
- Task 1 compact codec: implemented. Observation sequences are exact 14,544-byte Firestore `Bytes`; representative sequences are exact 48,480-byte `Bytes`. Strict envelope, numeric, all-101-phase, covariance PSD, corruption, unknown/missing-key, and round-trip tests pass.
- Task 2 compact persistence service: implemented. Basic persists 5 documents and High persists 9. Writes remain observation(s) → capture → revision → head, with one mutation per request and the head last. One generated opaque ID is reused for `profileId`, `captureSessionId`, and `revisionId`, closing the duplicate staged-chain orphan route. The executor fully validates a forged plan before any port call, ambiguous-write cleanup remains evidence-bound, the viewer performs exactly two reads, and deletion remains revision → capture → observations → head with resumable one-document mutations.
- Task 3 Firestore Rules: implemented statically. Immutable create validators enforce exact schemas, byte sizes, owner/path identity, canonical shared chain ID, Basic/High evidence, and the observation → capture → revision → head proof. The conservative AST counter now expands path interpolations; every create remains within the 700-node regression budget and every operation remains within 10 access calls. `tests/firestore-shooting-profile-rules.test.ts` passes 12/12. The real Firebase Rules compiler/Emulator owner/cross-owner behavior suite remains a mandatory external release gate and is not inferred from static tests.
- Task 4 clean CI: workflow added with read-only permissions, pinned checkout/setup-node commit SHAs, Node 22, exact pnpm 9.12.0, frozen install, separate typecheck/lint/hermetic-test/export steps, and triggers for pull requests, manual dispatch, and pushes to `main`. Local TypeScript passes. Direct ESLint had one pre-existing `TODAY'S` entity error on both prior `main` and the feature snapshot; this delivery escapes it. Local Expo export is not completion evidence because the quarantined install lacks the `react-native-css-interop` link. Clean Actions remains authoritative.
- Task 5 documentation and review: compact persistence design, validation protocol, and iPhone QA document the canonical shared ID, 5/9 documents, 14,544/48,480-byte envelopes, two-read viewer, head-last publication, direct deletion, and pending external gates. Known Critical findings are zero. Previously identified Important plan-validation, duplicate-chain, AST-estimation, and pnpm-build-policy findings were fixed with regression tests; final clean Actions and external gates remain pending.
- Latest local verification before delivery: `corepack pnpm check` passed; hermetic unit suite passed 378 tests with one intentional auth skip; focused service/integration passed 69/69; static Rules passed 12/12. Direct ESLint is rerun after the entity fix before remote publication.
- Task 6 delivery: the user explicitly authorized direct fast-forward publication to `main` on 2026-08-31. Remote commit/ref update and the resulting GitHub Actions inspection are pending at the time of this ledger snapshot.
- External release gates: Firebase Emulator, live Firebase integration, clean macOS prebuild/CocoaPods/Xcode, approved model SHA/license, bundle inspection, scientific held-out validation, and physical-iPhone acceptance remain pending. All three V2 flags remain exact-`1` opt-in and default off.
